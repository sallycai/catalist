<%= form_for(@goal) do |f| %>
  <div class="field">What would like to achieve? <br>
    <%= f.text_area :name, placeholder: "What is your goal?" %>
  </div>
  <%= f.submit "Create", class: "btn btn-primary" %>
<% end %>